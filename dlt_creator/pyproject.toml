[project]
name = "dlt_creator"
version = "0.1.0"
description = "DLT Creator"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["dlt_creator*"]