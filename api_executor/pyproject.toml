[project]
name = "api_executor"
version = "0.1.0"
description = "Library for Api Executions"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[tool.setuptools.packages.find]
include = ["api_executor*"]